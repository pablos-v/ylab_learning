version: '2.2'
services:
  db:
    image: postgres:latest
    container_name: coworking
    environment:
      POSTGRES_DB: coworking_db
      POSTGRES_USER: ylab
      POSTGRES_PASSWORD: ylabylab
    ports:
      - "5432:5432"
    volumes:
      # инициация БД
      - /create_db.sql
    restart: always